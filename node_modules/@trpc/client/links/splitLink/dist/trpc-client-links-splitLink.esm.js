function splitLink(opts) {
  return function (rt) {
    var left = opts.left(rt);
    var right = opts.right(rt);
    return function (props) {
      opts.condition(props.op) ? left(props) : right(props);
    };
  };
}

export { splitLink };
