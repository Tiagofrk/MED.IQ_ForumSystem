import { AnyRouter, HTTPResponseEnvelope, ProcedureType } from '@trpc/server';
import { LinkRuntimeOptions, PromiseAndCancel } from '../links/core';
export declare function httpRequest<TRouter extends AnyRouter = AnyRouter, TResponseShape extends HTTPResponseEnvelope<TRouter, unknown> = HTTPResponseEnvelope<TRouter, unknown>>(props: {
    runtime: LinkRuntimeOptions;
    type: ProcedureType;
    input: unknown;
    path: string;
    url: string;
    searchParams?: string;
}): PromiseAndCancel<TResponseShape>;
