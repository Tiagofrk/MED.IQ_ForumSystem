import { r as requestHandler } from '../../../dist/index-1a4e8243.esm.js';
import 'url';
import '../../../dist/once-a5ff19f1.esm.js';

/* eslint-disable @typescript-eslint/no-explicit-any */
function createExpressMiddleware(opts) {
  return (req, res) => {
    const endpoint = req.path.substr(1);
    requestHandler({ ...opts,
      req,
      res,
      path: endpoint
    });
  };
}

export { createExpressMiddleware };
